# Dev resolves assets from the same place production does, so a broken asset
# path fails here too. There is no local fallback: public/ holds no art.
VITE_ASSET_BASE=https://octothorpe-singularity-website.sfo3.cdn.digitaloceanspaces.com/v2
